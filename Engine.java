public class Engine {
    private GameBoard gameBoard;
    private int positionValue;

    public Engine(GameBoard gameBoard) {
        this.gameBoard = gameBoard;
    }

    public int evaluatePosition(GameBoard gameBoard) {
        for (int)
    }

    public Move findNextMove() {

    }
}
